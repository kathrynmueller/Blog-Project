<div class="media">
  <div class="media-left">

  	<%= link_to user_posts_path(name: comment.user.username) do%>

      	<%= image_tag comment.user.avatar.url, class: "avatar" %>
    <% end %>

  </div>
  <div class="media-body">
    <h4 class="media-heading"><%= comment.user.username %> says...</h4>
    <%= comment.comment_entry%>
  </div>
</div>